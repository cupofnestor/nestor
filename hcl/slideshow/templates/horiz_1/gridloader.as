package  {		import flash.display.*;	import flash.html.HTMLLoader;	import flash.net.URLRequest;		import flash.events.*;	import flash.display.BitmapData;	import flash.display.Bitmap;	import fl.video.VideoPlayer;		public class gridloader extends MovieClip {		var element:Object;		var slide:HTMLLoader;		var i:Number=0;		var isVid:MovieClip;				public function gridloader() {			slide = new HTMLLoader();            var urlReq:URLRequest = new URLRequest("index.html");            slide.width = stage.stageWidth;            slide.height = stage.stageHeight;            slide.load(urlReq);            	slide.addEventListener(Event.COMPLETE, loaded);			//slide.addEventListener(MouseEvent.MOUSE_UP, copy);			//addChild(slide);											}				private function loaded(e:Event):void		{			var copy:Sprite = e.target as Sprite;			var bmd:BitmapData = new BitmapData(stage.stageWidth,stage.stageHeight);			bmd.draw(copy);			var bm:Bitmap=new Bitmap(bmd);			addChild(copy);			//addChild(bm);						//addEventListener(Event.ENTER_FRAME, animate);			testforVideo();		}				private function testforVideo():void		{			element = slide.window.document.getElementById("airVid");			trace("element="+element);			if(element != null)			{				var wide:Number = element.getAttribute("width"); 				var high:Number = element.getAttribute("height");				var X:Number = element.getAttribute("x"); 				var Y:Number = element.getAttribute("y");												trace("Element position:"+X+","+Y);								var vid=element.getAttribute("src")				trace("non-null-element.src="+vid);				var req:URLRequest=new URLRequest(vid);				var player:VideoPlayer = new VideoPlayer();				addChild(player);				//player.x=X;				//player.y=Y;				player.width=wide;				player.height=high;									player.play(vid);							}								}				private function placeVid(e:Event):void		{			isVid =  e.target as MovieClip;			addChild(isVid);					}			}	}